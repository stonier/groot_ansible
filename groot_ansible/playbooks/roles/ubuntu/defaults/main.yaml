#################################################################################################
# Defaults
#################################################################################################

core_packages        : [eog, gimp, kile, mesa-utils, mplayer, net-tools, openssh-server,
                        synaptic, texlive-fonts-extra, texlive-fonts-recommended, 
                        texlive-xetex, unison-all-gtk, usbmount, vim, wget
                       ]
font_packages        : [fonts-unfonts-core, fonts-unfonts-extra, fonts-alee, fonts-baekmuk,
                        fonts-lexi-gulim, fonts-lexi-saebom,
                        fonts-liberation, fonts-liberation2, fonts-linuxlibertine,
                        fonts-nanum, fonts-nanum-coding, fonts-nanum-eco, fonts-woowa-bm,
                        ttf-dejavu
                       ]
packaging_packages   : [debhelper, gnupg, gnupg2, libssl-dev, libyaml-dev, packaging-dev,
                        python3-stdeb, python3-venv, python3-pip, python3-stdeb,
                        twine
                        ]
programming_packages : [build-essential, cmake, cmake-curses-gui, cppcheck, exuberant-ctags,
                        git, libnotify-bin, python3-dev, python3-pip, ttyrec]
