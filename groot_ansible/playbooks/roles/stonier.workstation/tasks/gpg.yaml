#################################################################################################
# GPG
#################################################################################################

- name: Create ~/.gnupg directory
  file:
    state: directory
    mode: 0700
    path: ~/.gnupg

- name: "Install/update gpg packages"
  apt:
    pkg: "{{ gpg_packages }}"
    state: latest
  become: yes
  tags: apt_install
